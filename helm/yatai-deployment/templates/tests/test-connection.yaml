apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yatai-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "yatai-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yatai-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
